<div class="d-flex justify-content-center align-items-center py-5 min-vh-100">
  <div class="bg-white rounded-4 p-4 shadow-lg border border-white"
       style="backdrop-filter: blur(40px); max-width: 900px; width: 95%; color: #212529;">

    <h1 class="archivo-black fw-bold fs-3 mb-4 text-center">Your Trip Details</h1>

    <div class="row mb-4 text-center text-md-start">
      <div class="col-md-6 mb-3">
        <p class="mb-1"><strong>Location:</strong></p>
        <p class="fs-5 text-primary"><%= h @trip.location %></p>
      </div>
      <div class="col-md-6 mb-3 d-flex justify-content-center justify-content-md-start gap-4 align-items-center">
        <div>
          <p class="mb-1"><strong>Start Date:</strong></p>
          <p><%= @trip.start_date.strftime('%B %d, %Y') %></p>
        </div>
        <div>
          <p class="mb-1"><strong>End Date:</strong></p>
          <p><%= @trip.end_date.strftime('%B %d, %Y') %></p>
        </div>
      </div>
    </div>

    <hr class="my-4">

    <% if @trip.trip_activities.any? %>
      <% activities_by_day = @trip.trip_activities.includes(:activity).group_by { |ta| ta.day || 1 } %>
      <% sorted_days = activities_by_day.keys.sort %>

      <div id="activitiesCarousel" class="carousel slide mt-5" data-bs-ride="false">
        <div class="carousel-inner">

          <% sorted_days.each_with_index do |day, index| %>
            <div class="carousel-item <%= 'active' if index == 0 %>">
              <div class="d-flex justify-content-between align-items-center mb-4">
                <% if sorted_days.size > 1 %>
                  <button class="btn btn-link text-dark" type="button" data-bs-target="#activitiesCarousel" data-bs-slide="prev">
                    <i class="bi bi-chevron-left fs-3"></i>
                  </button>
                <% else %>
                  <div style="width: 40px;"></div>
                <% end %>

                <h3 class="text-center m-0 flex-grow-1">Day <%= day %> — <%= (@trip.start_date + (day - 1)).strftime('%B %d, %Y') %></h3>

                <% if sorted_days.size > 1 %>
                  <button class="btn btn-link text-dark" type="button" data-bs-target="#activitiesCarousel" data-bs-slide="next">
                    <i class="bi bi-chevron-right fs-3"></i>
                  </button>
                <% else %>
                  <div style="width: 40px;"></div>
                <% end %>
              </div>

              <ul class="list-group mb-4">
                <% activities_by_day[day].sort_by(&:start_time).each do |ta| %>
                  <% activity = ta.activity %>
                  <li class="list-group-item mb-3 rounded-3 shadow-sm border border-light">
                    <div class="d-flex gap-3 align-items-start flex-wrap">

                      <% if activity.image_url.present? %>
                        <div class="activity-image">
                          <img src="<%= activity.image_url %>" alt="<%= activity.name %> image" />
                        </div>
                      <% end %>

                      <div class="flex-grow-1 d-flex flex-column gap-2">
                      <h5 class="text-primary fw-bold mb-1 d-flex align-items-center gap-2 flex-nowrap">
                        <%= h activity.name %>
                      </h5>

                        <p class="fst-italic text-muted mb-1"><%= h activity.address %></p>
                        <p class="mb-2"><%= h activity.description %></p>

                        <% if ta.start_time && ta.end_time %>
                          <p class="fw-semibold mb-1">
                            <i class="bi bi-clock me-1"></i>
                            <%= ta.start_time.strftime("%I:%M %p") %> - <%= ta.end_time.strftime("%I:%M %p") %>
                          </p>
                        <% end %>

                        <% if activity.cost.present? %>
                          <p class="fw-semibold text-dark mb-1">
                            <i class="bi bi-cash me-1"></i>
                            Estimated Cost: <%= h activity.cost %>
                          </p>
                        <% end %>
                      </div>
                    </div>
                  </li>
                <% end %>
              </ul>
            </div>
          <% end %>

        </div>
      </div>
    <% else %>
      <p class="text-muted fst-italic mt-4">No activities planned yet.</p>
    <% end %>

  </div>
</div>

<style>
  .activity-image img {
    border-radius: 0.5rem;
    width: 200px;
    height: 160px;
    object-fit: cover;
    box-shadow: 0 2px 6px rgba(0,0,0,0.15);
  }

  @media (max-width: 576px) {
    .list-group-item .d-flex {
      flex-direction: column !important;
      align-items: flex-start !important;
    }
    .activity-image img {
      width: 100%;
      height: auto;
      max-height: 300px;
    }
  }
</style>
